<p>Click on a composer's name to see all of the composer's works.</p>
<ul>
  <% @composers.each do |c| %>
    <li><%= link_to "#{c.first_name} #{c.last_name}", composer_path(c) %></li>
  <% end %>
</ul>
